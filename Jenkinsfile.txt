pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'dotnet build Estore.sln -p:configuration=release -v:n'   
            }
        }
        stage('Test'){
          steps  {
                sh 'dotnet test'
          }
       }
        
    }
}
